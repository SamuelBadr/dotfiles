# Retrieval architecture inherited from linear-algebra skill

BM25 + Snowball-stemmed navigation for `text`/`heading`, canonical-content delivery for labeled items. The full argument is in the `linear-algebra` skill's ADR-0001; the mechanism is copied verbatim (functions flagged `# shared with linear-algebra skill`). Recorded here so a reader of this skill alone isn't sent hunting for the rationale.

Rejected: vector/embedding RAG (math embeds poorly; the survey is fixed and labeled, so structure beats semantics).
