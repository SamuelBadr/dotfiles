#!/bin/sh

/opt/homebrew/bin/restic "$@"
status=$?

if [ "$status" -eq 3 ]; then
  printf '%s\n' 'Partial backup: some source paths were unreadable. Review backup.log; reporting failure to monitoring.' >&2
fi

exit "$status"
